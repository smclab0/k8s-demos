apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: trading-dashboard
  namespace: apps
  annotations:
    # Public ACME (Let's Encrypt/ZeroSSL) can't validate a hostname that
    # resolves to a private RFC1918 address -- using the cluster's internal
    # CA instead, same pattern every other app here already uses.
    cert-manager.io/cluster-issuer: lenny-internal-ca-issuer
spec:
  ingressClassName: traefik
  rules:
    # Replace <traefik-ip> with your traefik ingress controller's
    # external IP -- sslip.io resolves <name>.<ip>.sslip.io to <ip>.
    - host: trading.<traefik-ip>.sslip.io
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: trading-dashboard-lb
                port:
                  number: 80
  tls:
    - hosts:
        - trading.<traefik-ip>.sslip.io
      secretName: trading-dashboard-tls
